<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
    <NoWarn>$(NoWarn);NU1507</NoWarn>
  </PropertyGroup>
  <ItemGroup>
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="coverlet.msbuild" Version="10.0.1" />
    <!-- Held at 7.x: FluentAssertions 8.0+ requires a paid commercial license. -->
    <PackageVersion Include="FluentAssertions" Version="7.2.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageVersion Include="Microsoft.SourceLink.GitHub" Version="10.0.303" />
    <!-- Transitive pins (via Nuke.Common). Nuke.Common 10.1.0 resolves the NuGet client
         libraries at 6.12.x, but the .NET 10.0.4xx SDK's MSBuild targets bind to
         NuGet.Frameworks 7.9.0.0. The 6.12.x copy in the build project's output wins at
         load time and breaks in-process project evaluation (Project.HasPackageReference),
         which ITest uses. Pin the family to the version the SDK ships. This also clears
         GHSA-g4vj-cjjj-v7hg, which affects NuGet.Packaging 6.12.0-6.12.4. -->
    <PackageVersion Include="NuGet.Common" Version="7.9.0" />
    <PackageVersion Include="NuGet.Configuration" Version="7.9.0" />
    <PackageVersion Include="NuGet.Frameworks" Version="7.9.0" />
    <PackageVersion Include="NuGet.Packaging" Version="7.9.0" />
    <PackageVersion Include="NuGet.Versioning" Version="7.9.0" />
    <PackageVersion Include="Nuke.Common" Version="10.1.0" />
    <PackageVersion Include="ReportGenerator" Version="5.5.10" />
    <!-- Transitive pin (via Nuke.Common): Nuke.Common resolves a version with known
         advisories, so keep this at the latest 9.0.x patch. -->
    <PackageVersion Include="System.Security.Cryptography.Xml" Version="9.0.20" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />
  </ItemGroup>
</Project>